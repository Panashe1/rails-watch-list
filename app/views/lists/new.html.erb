<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, label: 'Name of your list', error: 'Please make a unique list' %>
    <%= f.button :submit, class:"btn btn-primary" %>
  <% end %>
</div>
